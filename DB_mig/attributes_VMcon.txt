# Get VM public id
az vm show \
  --resource-group mysql-migration-rg \
  --name mysql-linux-admin-vm \
  --show-details \
  --query publicIps \
  --output tsv

# Show NSG rules list
az network nsg rule list \
  --resource-group mysql-migration-rg \
  --nsg-name mysql-admin-vm2NSG \
  --output table

  # Find public ip of local machine
  curl -s https://ipv4.icanhazip.com
